pre-commit:
  parallel: true
  commands:
    typecheck:
      glob: "*.{ts,tsx}"
      run: npm run typecheck
    lint:
      glob: "*.{ts,tsx}"
      run: npx biome check --no-errors-on-unmatched {staged_files}
